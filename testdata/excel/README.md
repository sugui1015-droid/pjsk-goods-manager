# Real Excel Testdata

This directory is for local-only real Excel files used during migration.

- Real `.xlsx` files in this folder are ignored by Git.
- Keep the canonical source files here for parser verification.
- Expected parse results are tracked in `manifest.csv`.
