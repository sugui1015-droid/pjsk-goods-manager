# 2026-07-12 今日更新汇总

今天完成了《PJSK Goods Manager 后续开发总计划》的第一、二、三阶段,外加 Git 修复、换行符统一、文档对齐和迁移编号调查。所有改动均已提交并推送到 GitHub(`sugui1015-droid/pjsk-goods-manager`),工作区干净。

## 一、Git 修复与远程备份

- 确认无 Git 进程占用后,删除了残留的 `.git/index.lock`。
- 提交 `ad6838e`(付款详情与撤销审计)成功推送到 `origin/main`,未使用代理回退。

## 二、统一换行符(`896c149`)

- 新建 `.gitattributes`:Go/Vue/TS/JS/JSON/CSS/SQL/Markdown/YAML 用 LF;CMD/BAT/PS1 用 CRLF;XLSX、图片等标记 binary。
- `git add --renormalize .` 确认仓库原有文件本就是 LF,无业务代码变化。

## 三、项目状态与文档对齐(`9d56453`)

- `/api/config`:Excel 导入、付款流程两个模块标记为 `ready`(共 5 个模块全部 ready)。
- 重写 `docs/payment-workflow.md`:以当前代码为准 —— 管理员手动录入、创建即 approved、微信手续费本金 0.1% 向上进分、撤销后重录、用户截图审核流列为未实现的后续可选。
- 重写 `docs/normal-use-roadmap.md`:按真实代码列出已完成/未完成。
- 新增根目录 `HANDOVER.md`:16 个章节的完整交接文档(不含任何敏感信息)。

## 四、迁移编号只读调查(未改动任何文件)

- 迁移版本键 = 完整文件名;`schema_migrations` 查询确认两个 `0005_*.sql` 都已执行(07-11 08:07 / 17:40),互不冲突。
- 结论:历史迁移文件不重命名,新迁移从 `0013` 继续编号。

## 五、付款模块收尾(`4cc4dc9`)

- 录入付款区和付款详情撤销区新增"付款记录不可直接修改,请撤销后重新录入"提示。
- 写付款开发日志(含 40 个测试对边界条件的覆盖对照表、人工验收清单)。
- 撤销弹窗使用原生 prompt(),需在真实浏览器人工验收一次。

## 六、普通用户查询端增强(`44629fc`)

- `/query` 每条明细新增已付/剩余金额;订单和整体新增已付/未付汇总。
- 新增付款历史表(只含金额、方式、时间、状态;不暴露管理员信息;已撤销灰显删除线)。
- 新增登录限流:同一 IP 每分钟 20 次上限;同一 IP+CN 失败 5 次封锁 10 分钟。实测第 6 次错误返回 429。

## 七、管理员用户管理(`fa33982`)

- 新页面 `/admin/users`(列表:CN、查询码状态、订单数、总金额、已付、剩余)和 `/admin/users/:id`(详情:订单汇总、付款审计记录、导入来源)。

## 八、CN 合并(`ab06568`,迁移 `0013_cn_merge.sql`)

- 高风险功能,按计划带双重保护:先预览影响范围(迁移订单数、付款数),再必填原因 + 弹窗二次确认。
- 事务内迁移订单/付款/查询会话到目标 CN,源用户标记 `merged`,`cn_merge_logs` 永久审计(源/目标 CN、数量、原因、管理员、时间)。
- 已合并用户禁止再次作为源或目标(防循环合并)。

## 九、CSV 导出(`17d5e4b`)

- 三个导出接口:用户汇总、付款记录(保留当前筛选条件)、订单明细(含未付明细筛选)。
- UTF-8 BOM(Excel 中文不乱码)、金额两位小数、单次上限 50000 行。
- 用户管理、付款记录、订单查询三个页面新增导出按钮。

## 验证结果

- `go fmt` / `go build` / `go vet` / `go test ./...`:5 个包全部通过。
- `pnpm run build`(含 vue-tsc 类型检查):通过。
- 迁移 `0013` 已在本地库应用;新增管理端接口未登录访问均返回 401。

## 待人工验收(localhost:5173)

1. `/admin/payments` 撤销弹窗(原因输入 + 确认信息展示)。
2. `/admin/users` 列表、详情、CN 合并预览。
3. 三个 CSV 导出按钮,用 Excel 打开检查。
4. `/query` 用真实 CN + 查询码检查金额与付款历史。

## 下一阶段

按计划顺序:Excel 高级处理(差异导入、角色库、分类库)→ 系统管理(权限、审计日志、首页统计、自动测试)→ 正式部署。


## 十、开发日志与 Git 保护规则（2026/7/13 00:55:17，待提交）

- 修改目的：建立长期开发日志规则、仓库内钩子保护和日志目录说明，确保后续实际修改批次都有可追溯记录。
- 验收发现的问题：当天汇总日志已存在但仍未跟踪；仓库尚无统一的长期规则文件和日志删除/重命名保护钩子。
- 问题原因：此前日志依赖人工约定，缺少仓库级规则和提交前检查。
- 修改文件：`AGENTS.md`、`.githooks/pre-commit`、`docs/development-logs/README.md`、`docs/development-logs/2026-07-12-今日更新汇总.md`。
- 接口或数据库变化：无。
- 前端页面变化：无。
- 新增或调整的测试：新增 pre-commit 钩子检查；待执行新增日志允许测试和历史日志删除拒绝测试。
- 实际执行的验证命令及结果：`git config core.hooksPath .githooks` 已生效；使用临时 index 测试 `.githooks/pre-commit`，模拟新增日志退出码 0，模拟暂存删除历史日志退出码 1 并拒绝提交；真实暂存区已恢复为空。
- 是否修改数据库结构：否。
- 是否修改真实业务数据：否。
- Git 提交号：待提交。
- 后续仍需人工验收的内容：后续每次提交前确认实际代码变更和对应开发日志同批次纳入提交。


## 十一、2026-07-13 人工验收问题修复（2026/7/13 09:03:44，待提交）

- 修改目的：处理 2026-07-13 人工验收发现的问题，暂停后续阶段开发，优先修复付款页面金额总汇、订单明细展示、管理员导航、用户管理详情、未付明细 CSV 和 CN 合并入口展示。
- 验收发现的问题：付款录入页缺少订单总额/有效已付/剩余/手续费/实付总额总汇；用户管理快捷入口不完整；用户列表缺少顶部汇总；用户详情只有订单汇总没有订单明细；付款详情关联明细缺少角色、数量、单价、小计、已付、剩余；未付明细 CSV 使用 `payment_status=unpaid`，不能覆盖 partial，且不应依赖订单整体状态；谷子名称曾与分类拼接，导致分类独立列语义不清。
- 问题原因：部分页面沿用早期只读汇总字段；用户详情接口未返回明细数组；未付导出没有按 approved 付款本金计算 remaining；前端快捷导航只列出前三个入口；商品展示名历史上把 category 拼进名称。
- 修改文件：`backend/internal/payments/handler.go`、`backend/internal/users/handler.go`、`backend/internal/users/merge.go`、`backend/internal/export/handler.go`、`backend/internal/export/handler_test.go`、`backend/internal/orders/handler.go`、`backend/internal/query/handler.go`、`backend/internal/payments/void_integration_test.go`、`backend/internal/users/handler_test.go`、`frontend/src/App.vue`、`frontend/src/api/client.ts`、`frontend/src/style.css`、`docs/development-logs/2026-07-12-今日更新汇总.md`。
- 接口或数据库变化：未新增数据库迁移；扩展现有 JSON 响应字段，`/api/admin/users` 增加 `summary`，`/api/admin/users/:id` 的 orders 增加 `items`，`/api/admin/users/merge-preview` 增加 `move_query_session_count`，付款详情明细增加数量、单价、小计、已付、剩余；`/api/admin/export/order-items.csv` 支持 `unpaid_only=1` 按 `remaining_amount > 0` 导出。
- 前端页面变化：管理员快捷入口补齐付款记录和用户管理；付款录入页增加订单总额、有效已付总额、剩余待付总额、待付明细数、本次已选本金、微信手续费、本次实付总额；用户列表增加顶部汇总；用户详情展示订单及明细、角色、分类、数量、单价、小计、已付、剩余；付款详情关联明细补齐同一字段；未付 CSV 按 `unpaid_only=1` 请求。
- 新增或调整的测试：新增 export 包未付明细 CSV 数据库集成测试；补充 users handler 汇总、详情角色金额、未登录 401、合并预览拒绝错误测试；调整付款未付接口 summary 口径测试。
- 实际执行的验证命令及结果：`go fmt ./...` 通过；`go build ./...` 通过；`go vet ./...` 通过；`go test ./...` 通过；`pnpm.cmd run build` 通过；`git diff --check` 通过（仅提示 App.vue/style.css 下次 Git 接触时 CRLF 会转 LF，不是 diff-check 错误）。
- 是否修改数据库结构：否。
- 是否修改真实业务数据：否；新增测试使用隔离 CN 前缀并清理。
- Git 提交号：待提交。
- 后续仍需人工验收的内容：重新打开 `/admin/payments`、`/admin/users`、`/admin/users/:id`、付款详情、订单详情和未付明细 CSV，核对金额口径与角色字段展示。


## 十二、2026-07-13 长标识折叠与 Excel 导出格式化（2026/7/13 10:21:47，待提交）
- 修改目的：处理人工验收中发现的页面长技术标识直接展开、未付明细 CSV 排版较乱、各导出缺少统一 Excel 样式的问题。
- 验收发现的问题：用户详情、订单详情、付款录入/详情、导入历史/详情中 SKU、文件 SHA、来源位置等技术标识会直接占满表格；未付明细 CSV 无法保存列宽、边框、居中、冻结首行和筛选；现有导出只有 CSV，缺少格式化 Excel。
- 问题原因：早期页面把技术标识当作普通文本展示；导出层只使用 csv.Writer，没有统一的 XLSX 样式工具和格式化出口。
- 修改文件：`backend/internal/api/router.go`、`backend/internal/export/handler.go`、`backend/internal/export/xlsx.go`、`backend/internal/export/handler_test.go`、`backend/internal/payments/handler.go`、`backend/internal/users/handler.go`、`frontend/src/App.vue`、`frontend/src/api/client.ts`、`frontend/src/style.css`、`docs/development-logs/2026-07-12-今日更新汇总.md`。
- 接口或数据库变化：未新增数据库迁移；新增只读导出接口 `/api/admin/export/users.xlsx`、`/api/admin/export/payments.xlsx`、`/api/admin/export/order-items.xlsx`；付款未付明细、付款详情、用户详情的明细 JSON 额外返回 `product_id` 用于折叠标识展示。
- 前端页面变化：付款记录、用户管理、订单查询页面新增 Excel 主导出按钮并保留 CSV 次级入口；付款录入、付款详情、用户详情、订单详情、导入历史和导入详情中的 SKU、商品 ID、系列编码、来源位置、文件 SHA 默认折叠为“▶ 查看标识”，展开后显示“▼ 收起标识”和带字段名的可复制长文本。
- 新增或调整的测试：新增未付明细 Excel 导出集成测试，验证 remaining_amount > 0 过滤、partial 保留、paid 排除、voided 付款不计入已付并重新导出欠款、生成文件可作为 xlsx zip 读取、表头正确、金额单元格为数值、数量单元格为整数数值、表头/数据样式含边框居中、长文本列换行、冻结首行和自动筛选存在；保留 CSV 兼容测试。
- 实际执行的验证命令及结果：`pnpm.cmd run build` 通过；`go test ./internal/export` 通过；`go test ./internal/payments` 通过；`go build ./...` 通过；`go vet ./...` 通过；`go test ./...` 通过。`git diff --check` 通过（仅提示 `frontend/src/style.css` 下次 Git 接触时 CRLF 将转换为 LF，不是 diff-check 错误）。
- 是否修改数据库结构：否。
- 是否修改真实业务数据：否；新增测试使用 `EXPORT_UNPAID_TEST_%` 隔离前缀并在测试清理函数中删除。
- Git 提交号：待提交。
- 后续仍需人工验收的内容：在浏览器打开 `/admin/payments`、付款详情、`/admin/users/:id`、`/admin/orders/:id`、导入历史和导入详情，确认长标识默认折叠、展开和复制可用；下载三个 Excel 导出并用 Excel 检查边框、居中、列宽、筛选、冻结首行和金额数值格式。

## 十三、2026-07-13 技术标识展示收口与 Excel 导出路由修复（2026/7/13 11:05，待提交）
- 修改目的：处理人工验收发现的两个阻断问题：长技术标识仍在主要业务表格中影响阅读，以及 Excel 导出地址在人工访问时返回 404。
- 验收发现的问题：付款录入页和部分详情页仍可能在主表格或标题中直接展示 SKU、订单 ID、文件 SHA、导入批次 ID 等技术标识；导出按钮和接口需要确认当前运行 router 中真实注册，未登录时应返回 401 而不是 404，登录后应返回有效 xlsx。
- 问题原因：上一轮只做了单元格内折叠，仍会受窄列影响；前端导出仍使用 window.open 直接打开后端地址，认证和旧后端进程状态不够明确；人工 404 经路由测试和重启后端验证，属于旧运行进程/未加载新 router 的表现，而不是当前代码缺少路由。
- 修改文件：`frontend/src/App.vue`、`frontend/src/style.css`、`backend/internal/api/export_routes_test.go`、`docs/development-logs/2026-07-12-今日更新汇总.md`。
- 接口或数据库变化：未新增数据库迁移；导出接口仍为 `/api/admin/export/users.xlsx`、`/api/admin/export/payments.xlsx`、`/api/admin/export/order-items.xlsx`，其中未付明细使用 `unpaid_only=1`；补充路由级测试确认未登录返回 401、登录返回 200 和 xlsx Content-Type。
- 前端页面变化：付款录入页不再展示任何技术标识，只保留项目名、谷子名称、角色、分类、金额、状态、来源文件等业务字段；付款详情、用户详情、订单详情、导入历史、导入详情底部增加独立“技术标识”区域，默认收起，展开后以独立表格显示标识类型、对应业务内容、标识值和复制按钮；主表格和标题不再直接展示 UUID、SKU、文件 SHA、导入批次 ID、系列编码等长标识；导出改为带 `credentials: 'include'` 的 fetch 下载并创建本地 blob 文件。
- 新增或调整的测试：新增 `backend/internal/api/export_routes_test.go`，覆盖 users/payments/order-items/unpaid order-items 四个 xlsx 路由未登录非 404 且返回 401、登录后返回 200、Content-Type 为 xlsx、响应内容可被 zip 读取并包含 workbook/styles/sheet 部件。
- 实际执行的验证命令及结果：`go test ./internal/api` 通过；`go fmt ./...` 通过；`go build ./...` 通过；`go vet ./...` 通过；`go test ./...` 通过；`pnpm.cmd run build` 通过；`git diff --check` 通过（仅有 style.css CRLF 将在 Git 触碰时转 LF 的提示，不是 diff-check 错误）。
- 真实端到端下载验收：停止旧 8080 后端进程后使用 `backend/run.cmd` 启动新后端，`/health` 返回 200；未登录访问 `/api/admin/export/users.xlsx` 返回 401；使用临时管理员真实登录后下载 users、payments、unpaid-order-items、order-items 四个 xlsx 均返回 200、Content-Type 为 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，且均可作为 xlsx zip 读取；临时管理员和会话已清理，`API_EXPORT_%` 残留为 0。
- 浏览器按钮验收说明：前端页面已用临时管理员登录并确认导出按钮存在；Codex 内置浏览器无法稳定捕获程序化 blob 下载事件，且页面沙箱不允许 monkey patch fetch 记录调用，因此按钮点击的最终下载事件仍建议人工复验；后端真实文件响应和前端构建绑定路径已验证。
- 是否修改数据库结构：否。
- 是否修改真实业务数据：否；只创建并清理了临时管理员与会话，没有修改用户、订单、付款或导入业务数据。
- Git 提交号：待提交。
- 后续仍需人工验收的内容：重新打开 `/admin/payments`、付款详情、`/admin/users/:id`、`/admin/orders/:id`、导入历史和导入详情，确认技术标识只在底部独立区域展开；在浏览器中实际点击四个 Excel 导出按钮确认下载体验。
