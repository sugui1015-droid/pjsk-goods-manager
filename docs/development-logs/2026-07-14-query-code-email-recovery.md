# 2026-07-14 未登录用户查询码邮箱找回

## 阶段 1：现状调查与安全设计

- 阶段目标：在不修改业务代码的前提下核对 Git 基线，调查查询码登录/修改、邮箱验证码、匿名路由、前端登录页、限流和迁移机制，并确定第三阶段的数据模型与安全边界。
- Git 基线：分支为 `main`；`HEAD` 与 `origin/main` 均为 `fbdd98bb9b27e17495effa4b6e7145ea34ccc81b`；暂存区和已跟踪工作区为空；唯一未跟踪项为未读取、未修改、未处理的 `.claude/settings.local.json`。
- 查询码复用结论：查询码格式统一由 `internal/querycode.Validate` 校验，要求 6–32 位并仅允许字母、数字及 `- _ @ # .`；查询码使用 bcrypt `DefaultCost`，现有已登录修改流程禁止新旧相同，成功更新 `query_code_hash/query_code_updated_at/updated_at` 并在同一事务删除全部 `query_sessions`。本阶段将复用这些规则和算法。
- 查询登录与会话：登录对不存在用户使用 dummy bcrypt，并统一返回 CN 或查询码不正确；查询会话仅存 SHA-256 会话令牌哈希，过期或用户非 active 时不可继续使用。现有登录/绑定限流为单进程内存实现，只使用 `RemoteAddr`，明确不信任任意转发头，不能直接作为匿名找回的唯一限流。
- 邮箱验证码复用结论：复用现有 AES-GCM 邮箱解密/脱敏、`crypto/rand` 六位码生成模式、带密钥 HMAC、60 秒冷却、每小时 5 次、最多 5 次错误、事务行锁、发送前后两段事务、fake/SMTP 发送器和安全审计做法；不会把已登录邮箱验证验证码用于查询码重置。
- 数据库设计：`0018` 编号空闲。选择新增独立的查询码找回验证码表、短期重置会话表和匿名请求事件表，不扩展 `0017`。独立表和独立 HMAC 上下文形成结构性用途隔离；验证码绑定用户、当前 verified 邮箱版本和规范化 CN；重置令牌绑定用户、邮箱与固定用途，数据库只保存 HMAC。
- 匿名防滥用：请求事件持久化保存 CN 与客户端 IP 的带密钥 HMAC，不保存明文 IP；按 IP、CN、用户和邮箱执行窗口限制。继续只信任 `RemoteAddr`，不读取 `X-Forwarded-For`。公开请求对不存在、无邮箱、未验证、非 active、内部限流和发送失败均返回相同成功提示，并执行统一的最低响应时长以降低明显时序差异。
- 两步重置取舍：验证码确认只签发至少 32 字节随机、10 分钟有效、单次使用的短期令牌；查询码更新在第二个事务中重新锁定并确认用户和当前 verified 邮箱，验证令牌，拒绝与旧查询码相同，更新 bcrypt 哈希，删除全部查询会话，使所有找回验证码/令牌失效并写安全审计后一次提交。
- 邮件发送：复用现有 fake/SMTP 连接与安全校验，但新增用途明确的查询码找回发送方法和邮件模板；不会在邮件、响应、日志或审计中包含旧查询码、新查询码、Cookie 或会话令牌。
- 前端调查：登录页目前只有正常 CN+查询码登录和明确标注的首次绑定码流程；将新增独立“忘记查询码”三步内存状态。验证码、重置令牌和新查询码不进入 URL、localStorage、sessionStorage 或 Console，刷新/返回/切换流程时清空。
- 修改范围：本阶段只新增本专题日志，未修改业务代码、测试、迁移、页面或真实 `.env`。
- 测试命令：完成规则文件和源码调查；执行 Git 基线命令、迁移目录检查和源码搜索。阶段 1 不运行数据库写入测试。
- 测试结果：基线和调查检查通过；`0018` 未占用；暂存区为空。
- 测试数据：未创建；无需清理。未接触或修改真实业务数据，未连接真实 SMTP 或外部服务。
- 敏感信息检查：未读取真实 `.env`，未输出完整邮箱、验证码、查询码、重置令牌、Cookie、会话令牌、SMTP/数据库密码或密钥。
- 下一阶段：实现 `0018` 的三类持久化模型、状态/外键/索引/并发约束和迁移验证，再实现独立的验证码与重置令牌核心包。

## 阶段 2：迁移与持久化模型

- 阶段目标：新增查询码邮箱找回的独立持久化结构，确保验证码用途隔离、短期重置令牌可单次使用，并为匿名限流提供数据库权威事件。
- 修改文件：新增 `backend/migrations/0018_query_code_email_recovery.sql`。
- 数据库设计：新增 `query_code_recovery_request_events`，仅保存规范化 CN 与客户端 IP 的带密钥 HMAC 及时间；新增 `query_code_recovery_codes`，固定用途为 `query_code_recovery`，保存用户、当前邮箱版本、验证码 HMAC、客户端 IP HMAC、状态、有效期、错误次数和发送/使用/失效时间；新增 `query_code_recovery_sessions`，保存用户、邮箱版本、固定用途、重置令牌 HMAC、状态、有效期和消费/失效时间。
- 状态与约束：验证码状态为 `sending/active/used/locked/expired/invalidated/delivery_failed`，重置会话状态为 `active/used/expired/invalidated`；外键绑定用户和邮箱；部分唯一索引限制每个用户最多一个在途验证码和一个有效重置会话；索引覆盖 CN/IP 限流、用户/邮箱时间线和过期清理。
- 迁移安全：迁移只建表、约束和索引，不创建业务记录，不修改现有查询码、邮箱状态或用户状态，不删除历史数据；文件为 UTF-8 无 BOM。
- 验证命令：使用项目现有启动迁移器在临时端口启动后端；启动日志确认 `0018_query_code_email_recovery.sql` 已应用；`/health` 返回服务正常且数据库已连接；随后停止临时实例。
- 测试数据：迁移没有创建测试或业务数据，无需清理；未接触真实业务数据，未连接真实 SMTP。
- 敏感信息检查：迁移不包含完整邮箱、验证码、令牌、查询码、Cookie、会话值、数据库凭据或密钥。
- 下一阶段：实现独立 HMAC 上下文的验证码、重置令牌和匿名标识核心组件。

## 阶段 3：验证码与重置令牌核心

- 阶段目标：实现用途隔离的六位验证码、至少 32 字节随机重置令牌、带密钥 HMAC、常量时间比较、标识符 HMAC 和集中策略。
- 修改文件：新增 `backend/internal/querycoderecovery/security.go` 与 `security_test.go`。
- 安全设计：验证码使用 `crypto/rand` 拒绝采样；验证码 HMAC 同时绑定固定用途、用户、当前邮箱版本和规范化 CN；令牌由 32 字节随机数据编码，数据库只保存独立上下文 HMAC；CN/IP 限流键使用另一 HMAC 上下文；不同用途、用户、邮箱、CN 和标识类型不能互用。
- 策略：验证码与重置令牌均为 10 分钟有效，发送冷却 60 秒，每小时每 CN/邮箱最多 5 次，单 IP 每小时最多 20 次，验证码最多错误 5 次。
- 测试发现与修正：首次专项测试发现 CN HMAC 规范化只折叠空白但未与数据库不区分大小写的匹配语义一致；已修正为折叠空白后统一小写，并重跑通过。没有跳过或删除失败测试。
- 测试命令：`go fmt ./internal/querycoderecovery`；`go test -v ./internal/querycoderecovery`。
- 测试结果：验证码格式和随机源失败、用户/邮箱/CN 绑定、非法哈希、令牌长度/随机性/HMAC 隔离、标识符上下文和短密钥拒绝全部通过。
- 测试数据：纯单元测试，未访问数据库、未创建测试数据，无需清理；未接触真实业务数据或真实 SMTP。
- 敏感信息检查：测试不输出生成的验证码或令牌，不记录 HMAC、查询码、完整邮箱、Cookie、会话值、密码或密钥。
- 下一阶段：实现 PostgreSQL 存储与服务编排、专用邮件发送方法和匿名统一响应接口。
## 阶段 4：匿名请求验证码接口

- 阶段目标：实现 `POST /api/query/recovery/request`、用途专用邮件发送和持久化匿名防滥用，同时阻止公开响应泄露账号存在性或邮箱状态。
- 修改文件：新增 `internal/querycoderecovery/store.go`、`service.go`、对应服务/投递失败/存储集成测试，新增 `internal/query/query_code_recovery.go` 与 handler/HTTP 集成测试，新增 API 路由测试，新增 `recoveryemailverification/query_code_recovery_sender.go` 与测试；修改查询 handler 和 API 路由装配。
- 公开响应：有效 CN、不存在 CN、无资格、持久化限流和邮件投递失败均返回相同 HTTP 200 与固定提示；响应不包含邮箱、脱敏邮箱、用户 ID、邮箱 ID、资格状态或限流详情。对格式正确请求设置统一最低响应时长，降低明显的存在性时序差异。
- 限流：请求事件表对规范化 CN 与 `RemoteAddr` 客户端 IP 保存独立 HMAC；不信任 `X-Forwarded-For`。执行单 CN/邮箱冷却、每小时窗口和单 IP 每小时上限；即使 CN 不存在也记录持久化 IP/CN 事件。
- 发送边界：在短事务内锁定 active 用户和当前 verified 邮箱、执行限流、使旧验证码/令牌失效并创建 `sending` 记录；事务外解密并调用用途专用 Sender；成功后再次确认用户/邮箱/验证码状态再激活并审计。投递失败标记 `delivery_failed` 并写受控失败审计，公开响应仍保持统一。
- 邮件用途：fake 与 SMTP 使用独立 `SendQueryCodeRecovery` 方法和“查询码重置验证码”模板，明确不会自动登录；未改变已登录邮箱验证方法与模板。
+- 测试：公开响应一致性、无邮箱/状态字段、持久化 IP 限流、冷却、重发旧码失效、fake 投递、投递失败失效与审计、路由注册和 HTTP 合格/不存在 CN 交叉验证均通过。
+- 测试数据：仅 `TEST_QUERY_CODE_RECOVERY_*` 和保留示例域名；清理前订单、付款、CN 合并关联均为 0，测试结束清理用户、邮箱、验证码、重置会话、审计、查询会话、管理员和请求事件并回查。
+- 真实业务数据与外部服务：未修改真实业务数据，未连接真实 SMTP 或外部服务。
+
+## 阶段 5：验证码确认与重置令牌
+
+- 阶段目标：实现 `POST /api/query/recovery/verify`，确认 CN+验证码后签发短期、单次、用途/用户/邮箱绑定的重置令牌。
+- 验证事务：依次锁定用户、当前邮箱和最新查询码找回验证码；重新确认用户 active、邮箱仍为当前 verified 版本、用途固定、验证码 active 且未过期；错误尝试持久化，达到 5 次后锁定并写受控审计。
+- 用途隔离：查询码找回只查询独立表及固定 `query_code_recovery` 用途；测试证明已登录邮箱验证表中的验证码不能被该流程读取，不同用户、邮箱和 CN 之间不能互用。
+- 令牌：验证码正确后将验证码标为 used，使其他验证码和旧重置会话失效，生成至少 32 字节随机令牌，数据库仅保存独立 HMAC，令牌明文只在成功响应返回一次，10 分钟有效。
+- 公开错误：不存在、非 active、邮箱变化、用途错误、过期、已用、失效和错误验证码统一为安全拒绝；格式错误单独提示输入格式，成功响应不包含内部 ID、邮箱、验证码哈希或令牌哈希。
+- 测试：错误/过期/重发/跨 CN/用途隔离、锁定、用户停用、邮箱换绑/解绑、并发正确提交和令牌安全存储均通过。
+- 测试数据与敏感信息：测试夹具均已清理；未输出验证码、令牌或 HMAC，未接触真实业务数据。
+
+## 阶段 6：查询码重置事务
+
+- 阶段目标：实现 `POST /api/query/recovery/reset`，使用短期令牌安全更新查询码并一次性完成所有强制失效和审计。
+- 查询码规则：复用 `querycode.Validate` 和 bcrypt；要求非空、两次输入一致、格式为既有 6–32 位规则，并与现有已登录修改流程一致地禁止新旧查询码相同。
+- 事务边界：先由令牌 HMAC定位重置会话，再按稳定顺序锁定用户、当前邮箱和重置会话；重新确认用户 active、邮箱仍为当前 verified 版本、用途/绑定/有效期正确；更新 bcrypt 哈希和时间，删除全部查询会话，使所有未完成找回验证码及其他重置令牌失效，消费当前令牌并写 `query_code_recovery_completed` 审计后一次提交。
+- 联动失效：管理员停用用户、邮箱换绑、邮箱解绑和 CN 合并事务显式使相关未完成验证码与重置会话失效；即使状态直接变化，重置事务仍会再次验证用户和当前邮箱而拒绝旧流程。
+- 回归保护：没有修改一次性绑定码算法或入口；管理员会话表不在重置事务范围内；已登录修改查询码和登录限流仍使用原实现。
+- 测试：旧查询码失效、新查询码登录、全部旧查询会话失效、令牌重放失败、其他令牌失效、用户停用、邮箱换绑/解绑、两个并发重置仅一个成功，以及审计失败导致查询码/会话/令牌全部回滚均通过。
+- 测试命令：`go fmt` 覆盖相关包；`go test -v ./internal/querycoderecovery`；query/api/users/recoveryemailverification 相关专项与回归测试。
+- 测试结果：全部通过，没有 Skip、删除或绕过集成测试。
+- 测试数据：仅隔离前缀与保留示例域名；清理保护和清理后回查通过。未修改真实业务数据。
+- 敏感信息检查：API、日志和审计不记录或回显查询码、验证码、重置令牌、Cookie、查询会话令牌、完整邮箱、SMTP/数据库密码或密钥。
+- Git 收尾：`git diff --check` 通过；暂存区为空；真实 `.env` 差异为空；未发现临时工具、构建产物、非保留域邮箱或私钥。
+- 下一阶段：实现登录页独立“忘记查询码”入口及请求、验证、重置三步内存流程，并执行前端构建与响应式复核。
## 阶段 7：匿名三步前端流程与响应式布局

- 完成公开查询页“忘记查询码”独立入口，并与首次绑定入口明确区分。
- 完成 CN 请求、邮箱验证码校验、设置并确认新查询码的三步交互；重置授权仅保存在页面内存中，不渲染、不持久化，返回登录或刷新页面后清空。
- 页面不显示邮箱地址、验证码、重置令牌、查询码或账户资格状态；公开请求继续使用固定提示。
- 补充 720px、420px 窄屏布局，步骤、表单和按钮无横向溢出。
- pnpm.cmd run build 通过（包含 ue-tsc -b 与 Vite 生产构建）。
- git diff --check 通过；暂存区为空；真实 .env 差异为空；敏感模式检查未发现可疑内容。
- 当前基础 HEAD：$head；origin/main：$origin。
- 当前变更仅包含本阶段预期后端、迁移、测试、前端与本开发日志；.claude/settings.local.json 仍为未跟踪本地文件，未读取、未修改、未暂存。
- 未修改真实业务数据，未新增依赖，未产生或保留临时工具与测试凭据。

### 日志格式说明

- 阶段 4 至阶段 6 中若干行首的 +- 是追加记录时误带的排版字符；这些行均应按普通项目符号 - 理解，其事实内容保持有效。根据日志只追加规则，不回写或重排历史内容。
## 阶段 8：完整自动化验证

- `go fmt ./...` 通过，未产生找回邮箱第三阶段范围外的文件变化。
- `go build ./...`、`go vet ./...`、`go test ./...` 全部通过。
- `go test -v ./internal/recoveryemailverification` 通过。
- `go test -v ./internal/querycoderecovery` 通过，覆盖验证码与重置令牌隔离、限流、并发、回滚、状态失效和投递失败。
- `go test -v ./internal/query -run 'Recovery|QueryCode'` 通过，覆盖公开响应一致性、验证码校验、重置、旧会话失效与回归。
- `go test -v ./internal/api -run 'Recovery|QueryCode'` 通过，公开匿名路由和已登录邮箱验证路由均正常。
- `go test -v ./internal/users -run RecoveryEmail` 通过。
- `pnpm.cmd run build` 通过（`vue-tsc -b` 与 Vite 生产构建均成功）。
- 数据库集成测试使用保留测试前缀并在测试内清理；未修改真实业务数据。
- `git diff --check` 通过，暂存区为空；构建产物未进入 Git 状态。
- 下一步：普通 Chrome 人工验收，完成后清理测试数据、临时工具和临时凭据。
## 阶段 9：普通 Chrome 人工验收与清理

- 使用普通 Chrome、正式 Vue 页面、正式查询 handler、PostgreSQL 事务和仅限本机的 fake sender 完成验收；未使用 Network 面板，因此不声称读取 Network 面板。
- 公开请求：不存在 CN、无邮箱、pending 邮箱、disabled 用户、merged 用户均显示与合格账号一致的固定提示，不暴露账号存在性、邮箱或内部状态；快速重发仍使用安全统一提示。
- 正常闭环：合格测试账号完成发送、错误验证码安全拒绝、正确验证码进入第三步、两次新查询码不一致由前端阻止、重置成功返回登录页。
- 重置结果：旧查询码不能登录，新查询码可以登录；另一个主机名下预先建立的旧查询会话在重置后失效；原管理员会话保持有效。
- 状态变化：发码后用户变为 disabled、用户变为 merged、邮箱换绑、邮箱解绑、邮箱状态变为 disabled 时，旧验证码均被安全拒绝。
- 页面状态：刷新、返回登录、切换到首次绑定流程后，验证码、重置授权和新查询码状态均清空；浏览器检查确认 localStorage 与 sessionStorage 为空，URL 无验证码、重置令牌或邮箱参数。
- 回归：首次绑定码流程可以完成并使用新设置的查询码登录；已登录用户的 pending 找回邮箱面板正常，发送验证功能正常。
- 布局：1280px 与 375px 均无横向滚动，输入框和按钮未越界，按钮文字完整，三步流程未挤坏查询卡片。
- 页面安全：最终 DOM 未发现完整邮箱、邮箱密文、盲索引、验证码哈希、令牌哈希、重置令牌或内部用户 ID；本项目 Chrome Console 红色错误为 0。
- 临时验收工具仅位于 `backend/.tmp/query-code-recovery-acceptance/`，验收后已删除；临时测试键、日志和测试凭据均已删除，`backend/.tmp/` 回查为空。
- 清理前确认订单、付款和 CN 合并关联总计为 0；清理前本前缀测试范围共有 50 行（含 14 个测试用户和 1 个临时管理员及其安全记录）；清理后关联、测试行、测试用户和临时管理员均为 0。
- 测试数据仅使用 `TEST_QUERY_CODE_RECOVERY_` 前缀与保留示例域；未连接真实 SMTP，未修改真实业务数据。
- 真实 `.env` 未读取、未修改、未进入 Git 差异；`.claude/settings.local.json` 未读取、未修改、未暂存。
- 差异检查未发现非保留域完整邮箱、数据库连接串或密码、密钥、私钥、Cookie、Bearer 会话令牌；暂存区为空。
- 验收中一次 Chrome DOM 快照意外显示了两个已随机生成的临时测试查询码输入值；它们不属于真实业务凭据，相关测试账号和哈希已全部删除，且这些值未写入源码、Git 差异或开发日志。此后所有敏感输入均通过只读密码框和 Chrome 剪贴板传递。
- Chrome 控制组件自身出现过两次与项目无关且失败的遥测网络尝试；项目验收没有主动访问外部服务，也没有连接真实邮件服务。
- 本轮未使用子代理；未执行 `git add`、`git commit` 或 `git push`。
## 阶段 10：最终工作区保护复核

- 分支：`main`。
- HEAD：`fbdd98bb9b27e17495effa4b6e7145ea34ccc81b`。
- origin/main：`fbdd98bb9b27e17495effa4b6e7145ea34ccc81b`。
- 找回邮箱第三阶段共有 24 个预期修改/新增文件；无删除、无重命名、无无关业务模块变化。
- `git diff --check` 通过；暂存区为空；未执行提交或推送。
- 真实 `.env` 差异为空；迁移和本日志均为 UTF-8 无 BOM。
- `.claude/settings.local.json` 仍为未跟踪本地文件；仅通过 Git 状态确认，未读取、未修改、未删除、未移动、未暂存。
- 非保留域完整邮箱和高风险敏感模式检查均未发现；临时工具、临时凭据、构建产物未进入 Git 状态。
- 8080 与 5173 临时验收服务已停止。
- 下一步：等待用户复核；本轮不提交、不推送。